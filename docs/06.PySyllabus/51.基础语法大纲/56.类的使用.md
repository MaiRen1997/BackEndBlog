---
title: 类的使用
date: 2026-05-06 22:47:22
permalink: /pages/bd6bd6/
categories:
  - PySyllabus
  - 基础语法大纲
tags:
  - 
author: 
  name: Riverside Joy
  link: https://github.com/MaiRen1997
---
# 类

## 基础使用

### 定义类

1. 类型使用大驼峰

### 访问类的属性

1. `__dict__`访问, dict 可以查看所有属性

   1. ```python
      print(Human.__dict__)
      ```

2. 点的方式访问

   1. ```python
      class Human:
          """
          此类主要是构建人类
          """
          mind = '有思想'  # 第一部分：静态属性 属性 静态变量 静态字段
      print(Human.mind)  # 查
      ```

3. 使用getattr方法： `print(getattr(obj, 'mind', '空'))`

### 属性分类

1. 公开属性

2. 受保护属性

3. 私有属性

4. 魔法属性`__dict__之类的方法`

5. 类属性

   1. 接绑定在类本身上的变量，而不是绑定在类的实例（对象）上

   2. ```python
      class Student:
          # 类属性：所有实例共享
          school = "第一中学"      # 类属性
          total_students = 0       # 类属性
          
          def __init__(self, name):
              # 实例属性：每个对象独有
              self.name = name      # 实例属性
              Student.total_students += 1  # 修改类属性
      
      # 访问方式
      print(Student.school)        # 第一中学（通过类名访问）
      s1 = Student("张三")
      s2 = Student("李四")
      
      print(s1.name)               # 张三（实例属性）
      print(s1.school)             # 第一中学（通过实例也能访问类属性）
      print(s2.school)             # 第一中学（同一个值）
      print(Student.total_students) # 2（类属性记录了总人数）
      ```

6. 动态属性

   ```python
   class Student:
   
       def __init__(self, name, age):
           self.name = name
           self.age = age
   
   
   stu = Student('王大锤', 20)
   # 为Student对象动态添加sex属性
   stu.sex = '男'
   ```

   1. 不想用动态属性，使用`__slots__ = ('name', 'age')`，这样`Student`类的对象只能有`name`和`age`属性

   2. ```python
      class Student:
          __slots__ = ('name', 'age')
      
          def __init__(self, name, age):
              self.name = name
              self.age = age
      
      
      stu = Student('王大锤', 20)
      # AttributeError: 'Student' object has no attribute 'sex'
      stu.sex = '男'
      
      ```

   ```python
   class Demo:
       # 类级别的魔法属性示例
       __slots__ = ['public_attr', '_protected_attr']  # 可选
       
       def __init__(self):
           self.public_attr = "公开"           # 公开
           self._protected_attr = "受保护"      # 受保护
           self.__private_attr = "私有"         # 私有
       
       def __repr__(self):                     # 魔法方法
           return f"Demo({self.public_attr})"
   
   # 使用
   d = Demo()
   print(d.public_attr)      # ✅ 公开
   print(d._protected_attr)  # ⚠️ 可以访问，但约定不要这样做
   # print(d.__private_attr) # ❌ AttributeError
   print(d._Demo__private_attr)  # ⚠️ 可访问，但强烈不推荐
   ```

### 实例化

#### 实例化发生的三个步骤

1. 内存开辟空间
2. 将内存地址，传给`__init__`方法的第一个self参数
3. 执行init方法，如果内部有属性赋值给self操作，则进行赋值

## 对象内方法分类

### 实例方法

1. 直接在类中定义方法

2. 必须通过实例进行调用

3. 第一个参数是self

4. ```python
   class Person:
       def __init__(self, name, age):
           self.name = name
           self.age = age
   
       # 实例方法
       def greet(self):
           print(f"Hello, my name is {self.name} and I am {self.age} years old.")
   
   # 创建类的实例
   person = Person("Alice", 30)
   
   # 调用实例方法
   person.greet()  # 输出：Hello, my name is Alice and I am 30 years old.
   
   
   ```

### 类方法

1. 使用@classmethod装饰器

2. `类名.方法名`  或者 `实例.方法名`   调用

3. 第一个参数是cls

4. 用来访问、修改类属性

5. ```python
   class Person:
       population = 0  # 类属性
   
       def __init__(self, name, age):
           self.name = name
           self.age = age
           Person.population += 1  # 每次创建一个实例，人口数量增加
   
       # 类方法
       @classmethod
       def get_population(cls):
           return cls.population
   
   # 创建类的实例
   person1 = Person("Alice", 30)
   person2 = Person("Bob", 25)
   
   # 调用类方法
   print("当前人口数量:", Person.get_population())  # 输出：当前人口数量: 2
   print("当前人口数量:", person1.get_population())  # 输出：当前人口数量: 2
   
   ```

### 静态方法

1. 使用@staticmethod装饰器

2. 通过类名、实例   调用

3. 没有cls 或者self 参数

4. ```python
   class MathOperations:
       
       @staticmethod
       def add(x, y):
           return x + y
   
       @staticmethod
       def multiply(x, y):
           return x * y
   
   # 调用静态方法
   print("加法结果:", MathOperations.add(5, 3))  # 输出：加法结果: 8
   print("乘法结果:", MathOperations.multiply(5, 3))  # 输出：乘法结果: 15
   ```

### 抽象方法

1. 使用@abstractmethod装饰器

2. 定义
   1. 只有方法声明，没有具体实现
   2. 必须在子类中被重写（实现）
   3. 不能直接实例化包含抽象方法的类

3. 作用
   1. 定义接口规范
   2. 强制子类实现特定方法
   3. 实现多态性

4. ```python
   from abc import ABC, abstractmethod
   
   # 定义抽象基类
   class Shape(ABC):
       @abstractmethod
       def area(self):
           """计算面积（抽象方法）"""
           pass
       
       @abstractmethod
       def perimeter(self):
           """计算周长（抽象方法）"""
           pass
       
       # 可以有具体实现的方法
       def describe(self):
           return "这是一个形状"
   
   # 具体子类必须实现所有抽象方法
   class Rectangle(Shape):
       def __init__(self, width, height):
           self.width = width
           self.height = height
       
       def area(self):
           return self.width * self.height
       
       def perimeter(self):
           return 2 * (self.width + self.height)
   
   class Circle(Shape):
       def __init__(self, radius):
           self.radius = radius
       
       def area(self):
           import math
           return math.pi * self.radius ** 2
       
       def perimeter(self):
           import math
           return 2 * math.pi * self.radius
   
   # 使用
   rect = Rectangle(5, 3)
   print(f"矩形面积: {rect.area()}")        # 15
   print(f"矩形周长: {rect.perimeter()}")    # 16
   
   circle = Circle(4)
   print(f"圆形面积: {circle.area():.2f}")     # 50.27
   print(f"圆形周长: {circle.perimeter():.2f}") # 25.13
   
   # 尝试实例化抽象类会报错
   # shape = Shape()  # TypeError: Can't instantiate abstract class Shape
   
   ```

### 参数总结

1. 实例方法，第一个参数是self
2. 类方法，第一个参数是cls
3. 静态方法和抽象方法没有默认的参数

## 类空间

1. 对象查找属性的顺序：先从对象空间找 ------> 类空间找 ------> 父类空间找 ------->.....
2. 类名查找属性的顺序：先从本类空间找 -------> 父类空间找--------> ........

## 继承

### 继承分类

| 继承类型     | 写法                             | 关键点                     |
| :----------- | :------------------------------- | :------------------------- |
| **单继承**   | `class Child(Parent):`           | 使用 `super().__init__()`  |
| **多继承**   | `class Child(Parent1, Parent2):` | 注意多重继承顺序(MRO)      |
| **多层继承** | `class GrandChild(Child):`       | 多层 `super()` 调用        |
| **抽象基类** | `class Child(ABC):`              | 必须实现 `@abstractmethod` |

### 单继承

#### 执行顺序

1. 实例化对象时必须执行`__init__`方法,类中没有，从父类找，父类没有，从object类中找
2. 先要执行自己类中的eat方法，自己类没有才能执行父类中的方法。

#### 同时执行类、父类、实例方法

1. 父类.func

2. super().func()

3. ```python
   class Person(Aniaml):
       def __init__(self,name,sex,age,mind):
           '''
           self = p1
           name = '春哥'
           sex = 'laddboy'
           age = 18
           mind = '有思想'
           '''
           # Aniaml.__init__(self,name,sex,age)  # 方法一
           self.mind = mind
   
       def eat(self):
           super().eat()
           print('%s 吃饭'%self.name)
   ```

### 多继承

1. 遵守MRO顺序

2. ```python
   class A:
       def f1(self):
           print('in A') # 永远不会被执行
   
   class Foo(A):
       def f1(self):
           super().f1()    # 2. 调用 MRO 中 Foo 的下一个类：Bar.f1()
           print('in Foo') # 3. Bar.f1() 返回后执行
   
   class Bar(A):
       def f1(self):
           print('in Bar') # 3. 直接打印，没有 super()
   
   class Info(Foo,Bar):
       def f1(self):
           super().f1()        # 1. 调用 MRO 中 Info 的下一个类：Foo.f1()
           print('in Info f1') # 4. 最后执行
   
   obj = Info()
   obj.f1()
   
   '''
   in Bar
   in Foo
   in Info f1
   '''
   print(Info.mro())  # [<class '__main__.Info'>, <class '__main__.Foo'>, <class '__main__.Bar'>, <class '__main__.A'>, <class 'object'>]
   
   super()严格按照类的mro顺序执行
   
   ```

   1. MRO顺序： [Info, Foo, Bar, A, object]
   2. 流程分析
      1. 先执行Info的f1方法，
         1. 遇到super和print, 先执行super().f1()
         2. 发现没有执行完，继续递归调用(跳转到下面的第二条)
         3. print先不执行
      2. 根据MRO顺序，INFO的下一个类是Bar，所以执行Bar的f1方法
         1. 由于Bar的f1方法，没有super()方法，所以直接print
         2. 然后执行器交到上一层，也就是Info层
      3. Info层中的print得以执行，并打印数据
   3. **`这个执行顺序，并不是按照继承层级往上走，而是按照MRO顺序执行的`**


### 类的约束

1. 在父类定义方法，子类继承
2. 在元类定义抽象方法，各子类继承，然后重写抽象方法

### super的使用及MRO

#### super的执行顺序

1. **super是严格按照类的继承顺序执行**

### MRO的顺序

1. 单层继承的MRO顺序

2. 多层继承的MRO顺序

3. 改变Super的MRO的起点

4. ```python
   class A:
       def f1(self):
           print('in A')
   
   class Foo(A):
       def f1(self):
           super().f1()
           print('in Foo')
   
   class Bar(A):
       def f1(self):
           print('in Bar')
   
   class Info(Foo,Bar):
       def f1(self):
           super(Foo,self).f1()
           print('in Info f1')
   
   obj = Info()
   obj.f1()
   print(Info.mro()) #[<class '__main__.Info'>, <class '__main__.Foo'>, <class '__main__.Bar'>, <class '__main__.A'>, <class 'object'>]
   '''
   in Bar
   in Info f1
   '''
   ```

   1. super(Foo,self).f1() 的含义

      1. `self` 是 `Info` 实例

      2. `Foo` 是指定的起始查找类

      3. **从 MRO 中 `Foo` 的下一个类开始查找 `f1()` 方法，也就是调用了Bar的f1()方法**

      4. ```
         MRO: [Info, Foo, Bar, A, object]
                 ↑     ↑    ↑
                 跳过  起点 下一个
         
         super(Foo, self) → 从 Foo 之后开始：Bar
         ```

## 类的成员

### 公有/私有成员

1. 公有静态字段
   1. 哪三个地方可以访问

   2. ```python
      class C:
          name = "公有静态字段"
          
          
      C.name         # 类访问
      
      obj = C()
      obj.func()     # 类内部可以访问
      
      obj_son = D()
      obj_son.show() # 派生类中可以访问
      ```

2. 私有静态字段
   1. 如何定义

      1. 使用`__变量名` 进行定义

   2. 哪地方那个可以访问

      1. **类内部可以访问，外部不可访问**

   3. 类外如何访问

      1. 如果外部要访问，可以通过访问类的方式访问

      2. 通过下划线访问，是直接拼接类名，而不是通过`点的方式`

      3. ```python
         class C:
             __name = "私有静态字段"
             def func(self):
                 print (C.__name)
         obj = C()
         print(obj._C__name)
         ```

3. 定义getter/setter方法

   1. 使用双下划线`__`定义变量

   2. 使用`property`装饰器定义get方法

   3. 使用`@属性名.setter`装饰器定义set方法

   4. ```python
      class Student:
      
          def __init__(self, name, age):
              self.__name = name
              self.__age = age
      
          # 属性访问器(getter方法) - 获取__name属性
          @property
          def name(self):
              return self.__name
          
          # 属性修改器(setter方法) - 修改__name属性
          @name.setter
          def name(self, name):
              # 如果name参数不为空就赋值给对象的__name属性
              # 否则将__name属性赋值为'无名氏'，有两种写法
              # self.__name = name if name else '无名氏'
              self.__name = name or '无名氏'
      ```

4. 保护静态字段

   1. 如何定义

### 特性property

### isinstace 与issubclass

### 函数VS方法

## 内置双下方法

### `__len__`

### `__hash__`

### `__str__`

### `__repr__`

### `__call__`

### `__eq__`

### `__del__`

### `__new__`