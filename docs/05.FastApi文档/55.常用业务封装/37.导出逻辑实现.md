---
title: 导出逻辑实现
date: 2026-08-13 17:28:58
permalink: /pages/0ced8e/
categories:
  - FastApi文档
  - 常用业务封装
tags:
  - 
author: 
  name: Riverside Joy
  link: https://github.com/MaiRen1997
---
## 导出controller实现

1. 通过`get_safety_materials_list_services`查询并获取需要导出的数据
2. 通过`export_safety_materials_list_services`，将导出的数据传入，并生成excel

```python
@safety_materials_controller.post(
    '/export',
    summary='导出客货梯安全资料列表接口',
    description='用于导出当前符合查询条件的客货梯安全资料列表数据',
    response_class=StreamingResponse,
    responses={
        200: {
            'description': '流式返回客货梯安全资料列表excel文件',
            'content': {
                'application/octet-stream': {},
            },
        }
    },
    dependencies=[UserInterfaceAuthDependency('safety_materials:safety_materials:export')],
)
@Log(title='客货梯安全资料', business_type=BusinessType.EXPORT)
async def export_safety_materials_safety_materials_list(
    request: Request,
    safety_materials_page_query: Annotated[Safety_materialsPageQueryModel, Form()],
    query_db: Annotated[AsyncSession, DBSessionDependency()],
) -> Response:
    # 获取全量数据
    safety_materials_query_result = await Safety_materialsService.get_safety_materials_list_services(query_db, safety_materials_page_query, is_page=False)
    safety_materials_export_result = await Safety_materialsService.export_safety_materials_list_services(safety_materials_query_result)
    logger.info('导出成功')

    return ResponseUtil.streaming(data=bytes2file_response(safety_materials_export_result))
```

## 查询方法的实现

```python
class Safety_materialsDao:
    """
    客货梯安全资料模块数据库操作层
    """

    @classmethod
    async def get_safety_materials_detail_by_id(cls, db: AsyncSession, contract_no: str) -> SafetyMaterials | None:
        """
        根据合同号获取客货梯安全资料详细信息

        :param db: orm对象
        :param contract_no: 合同号
        :return: 客货梯安全资料信息对象
        """
        safety_materials_info = (
            (
                await db.execute(
                    select(SafetyMaterials)
                    .where(
                        SafetyMaterials.contract_no == contract_no
                    )
                )
            )
            .scalars()
            .first()
        )

        return safety_materials_info
```

## 导出成excel实现

```python
@staticmethod
    async def export_safety_materials_list_services(safety_materials_list: list) -> bytes:
        """
        导出客货梯安全资料信息service

        :param safety_materials_list: 客货梯安全资料信息列表
        :return: 客货梯安全资料信息对应excel的二进制数据
        """
        # 创建一个映射字典，将英文键映射到中文键
        mapping_dict = {
            'deliveryDate': '客户交期',
            'contractNo': '合同号',
            'customName': '客户名称',
            'safetyGearModelone': '安全钳型号1',
            'safetyGearSpeed': '安全钳速度',
            'safetyGearNo1': '安全钳编号1',
            'safetyGearNo2': '安全钳编号2',
            'safetyGearFactory': '安全钳制造单位名称',
            'safetyGearCertificationNo': '安全钳型式试验证书编号',
            'safetyGearUrl': '安全钳证书链接',
            'safetyGearCreateDate': '安全钳制造日期',
            'safetyGearCreateDate2': '安全钳制造日期2',
            'safetyGearCreateDate3': '安全钳制造日期3',
            'safetyGearCreateDate4': '安全钳制造日期4',
            'speedLimitModel': '限速器型号',
            'speedLimitSpeed': '限速器速度',
            'speedLimitNo1': '限速器编号1',
            'speedLimitNo2': '限速器编号2',
            'speedLimitFactory': '限速器制造单位名称',
            'speedLimitCertificationNo': '限速器型式试验证书编号',
            'speedLimitUrl': '限速器合格证链接',
            'speedLimitCreateDate': '限速器制造日期',
            'speedLimitCreateDate2': '限速器制造日期2',
            'bufferModel': '缓冲器型号1',
            'bufferModel2': '缓冲器型号2',
            'bufferSpeed': '缓冲器速度',
            'bufferNo1': '缓冲器编号1',
            'bufferNo2': '缓冲器编号2',
            'bufferNo3': '缓冲器编号3',
            'bufferNo4': '缓冲器编号4',
            'bufferBatch1': '缓冲器批次号1',
            'bufferBatch2': '缓冲器批次号2',
            'bufferFactory': '缓冲器制造单位名称',
            'bufferCertificationNo': '缓冲器型式试验证书编号',
            'bufferCertificationNo2': '缓冲器型式试验证书编号2',
            'bufferCertificationUrl': '缓冲器合格证链接',
            'bufferCreateDate': '缓冲器制造日期',
            'bufferCreateDate2': '缓冲器制造日期2',
            'ropeHeadModel': '绳头组合型号',
            'ropeHeadBatch': '绳头组合批次',
            'ropeHeadFactory': '绳头组合制造单位名称',
            'ropeHeadCertificationNo': '绳头组合型式试验证书编号',
            'ropeHeadCreateDate': '绳头组合制造日期',
            'machineModel': '主机型号',
            'machineSpeed': '主机速度',
            'machineNo': '主机编号',
            'machineFactory': '主机制造单位名称',
            'machineCertificationNo': '主机型式试验证书编号',
            'machineCertificationUrl': '主机合格证链接',
            'machineCreateDate': '主机制造日期',
            'upProtectModel': '上行超速保护装置制动器型号',
            'upProtectNoLeft': '上行超速保护装置制动器编号(左)',
            'upProtectNoRight': '上行超速保护装置制动器编号(右)',
            'upProtectFactory': '轿厢上行保护装置制造单位名称',
            'upProtectCertificationNo': '轿厢上行保护装置型式试验证书编号',
            'upProtectCreateDate': '轿厢上行保护装置制造日期',
            'moveProtectModel': '轿厢移动保护装置型号',
            'moveProtectNo1': '轿厢移动保护装置编号1',
            'moveProtectNo2': '轿厢移动保护装置编号2',
            'moveProtectFactory': '轿厢移动保护装置制造单位名称',
            'moveProtectCertificationNo': '轿厢移动保护装置型式试验证书编号',
            'moveProtectCreateDate': '轿厢移动保护装置制造日期',
            'controlCabinetModel': '控制柜型号',
            'controlCabinetNo': '控制柜编号',
            'controlCabinetFactory': '控制柜制造单位名称',
            'controlCabinetCertificationNo': '控制柜型式试验证书编号',
            'controlCabinetCreateDate': '控制柜制造日期',
            'certificationUrl': '合格证链接',
            'dataScope': '数据范围',
            'documentNO': '单据号',
            'menuCheckStrictly': '菜单树选择项是否关联显示',
            'deptCheckStrictly': '部门树选择项是否关联显示',
            'status': '角色状态',
            'delFlag': '删除标志',
            'createBy': '创建者',
            'createTime': '创建时间',
            'updateBy': '更新者',
            'updateTime': '更新时间',
            'remark': '备注',
        }
        binary_data = ExcelUtil.export_list2excel(safety_materials_list, mapping_dict)

        return binary_data

```

## 封装ExcelUtil

### __mapping_list:

1. 将list数据中的字段名映射为对应的中文字段名

### export_list2excel

1. 将需要导出的list数据转化为对应excel的二进制数据

```python
import io

import pandas as pd
from openpyxl import Workbook
from openpyxl.styles import Alignment, PatternFill
from openpyxl.utils import get_column_letter
from openpyxl.worksheet.datavalidation import DataValidation


class ExcelUtil:
    """
    Excel操作类
    """

    @classmethod
    def __mapping_list(cls, list_data: list, mapping_dict: dict) -> list[dict]:
        """
        工具方法：将list数据中的字段名映射为对应的中文字段名

        :param list_data: 数据列表
        :param mapping_dict: 映射字典
        :return: 映射后的数据列表
        """
        mapping_data = [{mapping_dict.get(key): item.get(key) for key in mapping_dict} for item in list_data]

        return mapping_data

    @classmethod
    def export_list2excel(cls, list_data: list, mapping_dict: dict) -> bytes:
        """
        工具方法：将需要导出的list数据转化为对应excel的二进制数据

        :param list_data: 数据列表
        :param mapping_dict: 映射字典
        :return: list数据对应excel的二进制数据
        """
        mapping_data = cls.__mapping_list(list_data, mapping_dict)
        df = pd.DataFrame(mapping_data)
        binary_data = io.BytesIO()
        df.to_excel(binary_data, index=False, engine='openpyxl')
        binary_data = binary_data.getvalue()

        return binary_data

    @classmethod
    def get_excel_template(cls, header_list: list, selector_header_list: list, option_list: list[dict]) -> bytes:
        """
        工具方法：将需要导出的list数据转化为对应excel的二进制数据

        :param header_list: 表头数据列表
        :param selector_header_list: 需要设置为选择器格式的表头数据列表
        :param option_list: 选择器格式的表头预设的选项列表
        :return: 模板excel的二进制数据
        """
        # 创建Excel工作簿
        wb = Workbook()
        # 选择默认的活动工作表
        ws = wb.active

        # 设置表头文字
        headers = header_list

        # 设置表头背景样式为灰色，前景色为白色
        header_fill = PatternFill(start_color='ababab', end_color='ababab', fill_type='solid')

        # 将表头写入第一行
        for col_num, header in enumerate(headers, 1):
            cell = ws.cell(row=1, column=col_num)
            cell.value = header
            cell.fill = header_fill
            # 设置列宽度为16
            ws.column_dimensions[chr(64 + col_num)].width = 12
            # 设置水平居中对齐
            cell.alignment = Alignment(horizontal='center')

        # 设置选择器的预设选项
        options = option_list

        # 获取selector_header的字母索引
        for selector_header in selector_header_list:
            column_selector_header_index = headers.index(selector_header) + 1

            # 创建数据有效性规则
            header_option = []
            for option in options:
                if option.get(selector_header):
                    header_option = option.get(selector_header)
            dv = DataValidation(type='list', formula1=f'"{",".join(header_option)}"')
            # 设置数据有效性规则的起始单元格和结束单元格
            dv.add(
                f'{get_column_letter(column_selector_header_index)}2:{get_column_letter(column_selector_header_index)}1048576'
            )
            # 添加数据有效性规则到工作表
            ws.add_data_validation(dv)

        # 保存Excel文件为字节类型的数据
        file = io.BytesIO()
        wb.save(file)
        file.seek(0)

        # 读取字节数据
        excel_data = file.getvalue()

        return excel_data

```

