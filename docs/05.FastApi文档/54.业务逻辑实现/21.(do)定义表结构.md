---
title: (do)定义表结构
date: 2026-07-05 12:46:18
permalink: /pages/c1fb0e/
categories:
  - FastApi文档
  - 业务逻辑实现
tags:
  - 
author: 
  name: Riverside Joy
  link: https://github.com/MaiRen1997
---
## 类型分类

```python
from sqlalchemy import CHAR，BigInteger，String, Integer, DateTime, Date, JSON, Column
```

1. 字符：CHAR
2. 字符串：String
3. 数字：Integer、SmallInteger、BigInteger
4. 日期时间：DateTime
5. 日期：Date
6. JSON：JSON
7. 

## 完整代码

```python
from sqlalchemy import String, Column, SmallInteger, DateTime, BigInteger

from config.database import Base


class UserSessions(Base):
    """
    用户会话关联表
    """

    __tablename__ = 'user_sessions'
    __table_args__ = {'comment': '用户会话关联表'}

    id = Column(BigInteger, primary_key=True, autoincrement=True, nullable=False, comment='主键ID')
    user_id = Column(String(32), nullable=False, comment='用户ID')
    session_id = Column(String(64), nullable=False, comment='会话ID')
    created_at = Column(DateTime, nullable=False, comment='创建时间')
    is_active = Column(SmallInteger, nullable=True, comment='是否仍在会话中: 1-是, 0-否')
```

