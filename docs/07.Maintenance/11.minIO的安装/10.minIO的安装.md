# 单机部署为例。

------

# 1. 创建存储目录

例如：

```bash
mkdir -p /data/minio
mkdir -p /data/minio-data
```

目录说明：

```bash
/data/minio         # MinIO程序
/data/minio-data    # 对象存储数据
```

------

# 2. 下载 MinIO

进入安装目录

```bash
cd /data/minio
```

下载最新版

```bash
wget https://dl.min.io/server/minio/release/linux-amd64/minio
```

如果 wget 不行，可以使用 curl

```bash
curl -O https://dl.min.io/server/minio/release/linux-amd64/minio
```

赋予执行权限

```bash
chmod +x minio
```

移动到系统目录

```bash
mv minio /usr/local/bin/
```

检查是否安装成功

```bash
minio --version
```

应该看到类似

```bash
minio version RELEASE.2025-xx-xx
```

------

# 3. 创建运行用户（推荐）

不要使用 root。

```bash
useradd -r minio -s /sbin/nologin
```

授权目录

```bash
chown -R minio:minio /data/minio-data
```

------

# 4. 创建配置文件

创建

```bash
vim /etc/default/minio
```

写入

```bash
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=12345678

MINIO_VOLUMES="/data/minio-data"

MINIO_OPTS="--console-address :9001"
```

说明：

- API端口：9000
- 控制台：9001

------

# 5. 创建 systemd 服务

创建

```bash
vim /etc/systemd/system/minio.service
```

内容：

```bash
[Unit]
Description=MinIO
After=network.target

[Service]
User=minio
Group=minio

EnvironmentFile=/etc/default/minio

ExecStart=/usr/local/bin/minio server $MINIO_VOLUMES $MINIO_OPTS

Restart=always

LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
```

------

# 6. 重新加载 systemd

```bash
systemctl daemon-reload
```

设置开机启动

```bash
systemctl enable minio
```

启动

```bash
systemctl start minio
```

查看状态

```bash
systemctl status minio
```

如果看到

```bash
Active: active (running)
```

说明启动成功。

------

# 7. 开放防火墙

CentOS7 默认 firewalld：

```bash
firewall-cmd --permanent --add-port=9000/tcp
firewall-cmd --permanent --add-port=9001/tcp

firewall-cmd --reload
```

如果使用云服务器，还需要在云厂商安全组开放：

```bash
9000
9001
```

------

# 8. 登录控制台

浏览器访问：

```bash
http://服务器IP:9001
```

例如：

```bash
http://192.168.1.100:9001
```

登录：

```bash
用户名：admin
密码：12345678
```

登录成功后即可：

- 创建 Bucket
- 上传文件
- 创建用户
- 创建 AccessKey
- 设置权限

------

# 9. S3 API 地址

程序访问：

```bash
http://服务器IP:9000
```

例如 Python：

```bash
from minio import Minio

client = Minio(
    "192.168.1.100:9000",
    access_key="admin",
    secret_key="12345678",
    secure=False
)
```

Java、Go、Node.js、JavaScript 等 SDK 也都兼容 Amazon S3 API。

------

# 10. 常用命令

查看日志：

```bash
journalctl -u minio -f
```

停止：

```bash
systemctl stop minio
```

重启：

```bash
systemctl restart minio
```

开机启动：

```bash
systemctl enable minio
```

取消开机启动：

```bash
systemctl disable minio
```

------

# 生产环境建议

如果你计划将 MinIO 用于生产环境（例如 FastAPI + Vue 文件上传、AI 知识库存储、对象存储等），建议进一步配置：

- 使用 **Nginx** 反向代理，将 `9000/9001` 映射到 `80/443`。
- 配置 **HTTPS**（TLS 证书）。
- 修改默认的 `admin` 用户名和密码。
- 使用独立磁盘（如 `/data1`、`/data2`）存储数据。
- 配置定期备份。
- 如果数据量较大或需要高可用，采用官方推荐的 **多节点多磁盘（Distributed）** 部署，而不是单节点。

如果你的服务器还需要部署 **FastAPI + PostgreSQL + Redis + MinIO + Nginx**，我也可以给你提供一套适合 CentOS 7 的完整生产环境部署方案。