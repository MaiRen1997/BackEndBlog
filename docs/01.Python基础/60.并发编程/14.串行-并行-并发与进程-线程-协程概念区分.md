## 串行-并行-并发

### 核心定义

| 概念     | 定义                                                       | 关键特征                                 |
| :------- | :--------------------------------------------------------- | :--------------------------------------- |
| **串行** | 任务一个接一个执行，有先后顺序                             | 单车道，车一辆一辆过                     |
| **并行** | 多个任务在同一时刻同时执行                                 | 多车道，多辆车同时并行                   |
| **并发** | 多个任务在同一个时间段内交替执行（宏观同时，微观可能串行） | 单车道但多辆车交替穿插通过（或分时复用） |

### 简单比喻

- **串行**：一个人吃三碗面 → 一碗吃完再吃下一碗。
- **并行**：三个人各吃一碗面 → 同时吃。
- **并发**：一个人吃三碗面，快速轮换着吃（每碗吃一口） → 感觉在三碗同时吃。

## 进程-线程-协程

### **核心区别**

- **进程**：资源分配的最小单位（拥有独立内存空间）。
- **线程**：CPU调度的最小单位（共享进程的内存空间）。
- **协程**：用户态手动调度的轻量级“伪线程”（更轻量、切换成本极低）。

### 1. 一句话比喻（接着刚才吃面的例子）

假设有一家**餐厅**（一个程序）：

- **进程** = 一个**独立厨房**（有自己的灶台、水池、冰箱）。
- **线程** = 厨房里的**厨师**（共用厨房里的设备和食材）。
- **协程** = 一个厨师**手上有多个菜**，他快速来回炒（不用把锅铲交给别人再拿回来，自己决定先做哪道菜的下一步）。

```sh
进程
  └── 线程1
  │      ├── 协程A
  │      └── 协程B
  └── 线程2
```

